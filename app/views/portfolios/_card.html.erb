<div class="col-md-4">
  <div class="card p-3 mb-2" style="min-height: 330px">
    <div class="d-flex justify-content-between pb-4"  style="border-bottom: #E8EAF6 1px solid">
      <div class="d-flex flex-row align-items-center">
        <% if portfolio.user.avatar.attached? %>
          <%= image_tag(portfolio.user.avatar, class: 'img-fluid avatar-img-mini', alt: 'Аватарка') %>
        <% else %>
          <%= vite_image_tag 'images/default_avatar.jpg', alt: "Аватарка", class: "img-fluid avatar-img-mini" %>
        <% end %>
        <div class="ms-2 c-details">
          <h6 class="mb-0"><%= portfolio.title.presence || portfolio.user.short_name %></h6>
          <span class="mb-0"><%= "@#{portfolio.user.login}" %></span><br>
          <% if portfolio.user.last_seen_at.present? %>
            <span><%= last_time(portfolio.user.last_seen_at) %></span>
          <% end %>
        </div>
      </div>
      <div class="badge"><span><%= portfolio.user.role_name %></span></div>
    </div>
    <div class="mt-2 pb-2" style="border-bottom: #E8EAF6 1px solid">
      <h3 class="heading"><%= portfolio.user.position.presence %></h3>
      <div>
       
      </div>
        <div class="mt-3">
          <div class="mt-3">
            <span class="text1">Проектов: <span class="text2"><%= portfolio.user.projects.count %></span></span><br>
            <span class="text1">Сертификатов и наград: <span class="text2"><%= portfolio.awards.count %></span></span>
          </div>
        </div>
    </div>
    <p class="mt-3">
      <%= link_to 'Посмотреть', portfolio_path(portfolio), class: 'btn btn-outline-secondary' %></p>
  </div>
</div>
